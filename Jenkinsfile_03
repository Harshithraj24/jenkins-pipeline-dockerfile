#!groovy

pipeline {
    environment{
        dockerimagename = "harshithraj24/javaapp"
        dockerImage=""
    }


       stage('Build image') {
            steps{
           script {
          dockerImage = docker.build dockerimagename
        }
      }
    }


    post {
        always {
            cleanWs()
        }
    }
}
