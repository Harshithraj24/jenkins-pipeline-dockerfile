#!groovy

pipeline {
    environment{
        dockerimagename = "harshithraj24/javaapp"
        dockerImage=""
    }

//     agent {
//         dockerfile {
//             label "docker"
//         }
//     }

//     stages {
//         stage("Build") {
//             steps {    
//                 sh "mvn -version"
//                 sh "mvn clean install"
//             }
//         }
       stage('Build image') {
            steps{
           script {
          dockerImage = docker.build dockerimagename
        }
      }
    }
    }

    post {
        always {
            cleanWs()
        }
    }
}
